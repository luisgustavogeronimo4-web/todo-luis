import { createClient } from "@supabase/supabase-js"; // Variáveis de ambiente definidas no arquivo .env (serão preenchidas pelo usuário) const supabaseUrl = import.meta.env.VITE_SUPABASE_URL?.trim(); const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY?.trim(); // Se alguma das variáveis ainda estiver vazia, usamos valores de fallback que são válidos sintaticamente // (o cliente ainda será criado, mas não disparará a exceção de URL inválida). // O desenvolvedor deve substituir esses valores pelas credenciais reais antes de usar a app. const safeUrl = supabaseUrl || "http://localhost:54321"; const safeKey = supabaseAnonKey || "anon-key-placeholder"; export const supabase = createClient(safeUrl, safeKey);